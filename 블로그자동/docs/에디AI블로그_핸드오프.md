# 에디 AI 블로그 핸드오프

## 2026-08-07 — ★트래픽 붕괴 원인 규명 + 연예 파이프라인 3중 버그 수정

**에디님 질문**: "애드포스트 90만원(하루 3천~1만 방문자) 벌었는데 지금 하루 500 조회수도 안 나온다."

**원인(발행 이력·색인·순위로 역추적. 통계 API 쿠키 만료로 일별 수치는 미확인)**
- 저품질·검색제외 아님. 최근 90편 전부 `searchYn=true`·전체공개, 색인 정상. **블로그 지수도 살아 있다** —
  "무료 AI 이미지 툴" 1위, "AI 글감" 6위, "시니어 블로그 교육" 7위 노출 확인.
- 진짜 원인 = **트래픽 엔진(연예 이슈 글)을 껐다.** 월별 발행: 2026-03 148편(전부 연예) /
  **04월 250편(피크)** / 05월 184 / 06월 132 / 07월 87(연예 67 + AI 18) / **08월 7일간 21편(AI 15, 연예 3)**.
  연예 이슈 글은 유입 수명이 2~3일뿐이라 **재고가 안 쌓인다** → 끊는 순간 그날부터 0에 수렴.
- 보조 원인: 새 AI 글이 head 키워드("AI 부업"·"정부지원금 신청"·"제습기 추천") 전부 미노출.
  **좁은 롱테일에선 1위가 나오므로 키워드 선정을 롱테일로 돌려야 한다.**

**연예 글이 8/4 이후 0편이 된 이유 = 자동화 3중 버그(전부 이날 수정)**
1. 🔴`gen_celeb_ai.py:651`이 정의되지 않은 `art_title`을 참조(NameError) → **연예편이 캡처 단계에서 100% 사망**
   (8/4 새벽 로그에서 확인). `art_title`을 `art` 직후 지역변수로 정의 + `.format` 호출 3곳 통일.
2. 🔴**금칙 기사를 풀에서 안 빼서 같은 기사를 슬롯마다 반복 선택** → 8/7 새벽 연예 3슬롯이 전부
   '오은영 살 빠진' 기사 하나로 스킵(연예 0편). 실패한 인물은 `exclude_persons`에 안 들어가는 게 원인.
   → **`build_pure_pool`이 `_BLOCK_RE` 기사를 풀 단계에서 제외**(검증: 금칙 7건 제외, 후보 34건 유지).
3. `_clean_title`의 `.strip("\"'")`가 **제목 맨 앞 인용부호만 떼어내 제목을 깨뜨렸다** —
   «'코인 부부', 이호선…» → «코인 부부', 이호선…», «'OO' 화보가 화제…» → «화보가 화제…»(주인공 소실).
   발행된 글 다수가 이 상태(CTR 손실). → **감싼 따옴표만 제거 + 홀수 개면 여는 따옴표 복원**.

**신규 도구**: `run_celeb_one.py <소재…>` = 소재를 지정해 연예 글만 N편 생성·임시저장.
  `celeb_ai10` 모드는 `PICK_PROMPT_PURE`를 쓰는데 그 프롬프트에 hint 자리가 없어 소재 지정이 안 먹는다
  → **풀을 소재로 사전 필터(`_pool_for_hint`, 별칭 이숙캠/나솔 포함 + 금칙 제외)해서 넘긴다.**

**★미해결(다음 세션 최우선): 연예 사진 검증이 프로그램을 안 본다**
- `_face_ok`는 '이름'만 검증 → 이호선이 나온 **옛 JTBC 토크쇼(성재기 출연 회차)** 캡처가 통과해
  이혼숙려캠프 글의 대표사진으로 들어갔다(실측).
- 신설한 익명 경로(`fetch_multi(anon=True)`, 일반인·가명 출연자용 — `_is_anon_person`)는 **얼굴 또렷함만** 보므로
  '30기/31기 막방 LIVE' **예고 그래픽**과 **다른 기수·다른 출연자**(솔로 리액션 광수)까지 통과했다.
- → 필요한 것: 프레임이 **지정 프로그램 화면인지**(로고·자막·세트) + **예고 그래픽이 아닌지** +
  **기수 불일치 아닌지**를 비전으로 판정하는 `_scene_ok`. 이거 없이는 연예편 사진을 신뢰할 수 없다.
- 캡처 0장일 때 제미나이 생성 썸네일 폴백은 **연예편에서 차단**(08-02 '가짜 부부' 사고 재발 방지) → 텍스트 썸네일.

**오늘(8/7) 배치 실측**: 계획 6편(AI2+쇼핑1+연예3) → **발행 3편**(05:21·06:51·08:01 예약발행, 전부 공개 확인).
연예 3편은 위 2번으로 전멸. 추가 관찰: "오늘의 주제 0개 도출" → 소재뱅크 폴백(홈판 트렌드 주제 추출 실패, 별건 과제).
8/6 배치는 08:38 `ERR_INTERNET_DISCONNECTED`로 04편에서 중단 → 05·06편 미실행(네트워크 재시도 없음, 과제).

# 에디 AI 블로그 — 작업 핸드오프 (새 창에서 이어가기)

> 2026-07-19. 이 창이 꽉 차서 새 창으로 이관. 새 창은 `~/홈판자료`(또는 `~/클로드 코드 블로그 자동화 웹`)에서 열고 이 파일 + 메모리(MEMORY.md)를 먼저 읽으면 100% 이어감.

## ★★ 현재 상태 요약 (2026-07-31 밤 — 새 창은 여기부터 + 스킬 `eddie-blog-auto` 읽기)
- **블로그** = "AI 에디"(ioiykd8599). **절대 1순위 = 네이버 홈판 노출 → 애드포스트 수익.** 제목·썸네일·도입부 무조건 홈판 공식. 브랜딩·판매 X.
- **하루 5편, 새벽 4:30 자동**(`com.edi.aiblog`, caffeinate·**편당 30분 stagger**=AI 티 방지): **AI 홈판 4편**(gen_ai, 홈판검증 주제) + **연예 90/AI10 1편**(gen_celeb_ai mode=celeb_ai10, 1순위 소재·사망범죄비방 제외).
- **전부 임시저장까지만.** 에디님이 연예 사진 삽입·검토 후 발행. 인사말('안녕하세요 에디입니다') 제거됨.
- 공식: 제목·썸네일=전체 홈판(dir=0) / AI본문=dir30 / 연예본문=dir12. 연예 사진=유튜브 방송 캡처(자막OK·기사아님) + 자리표시자.
- **첫 자동 실행 = 2026-08-01 04:30.** 로그 `~/클로드 코드 블로그 자동화 웹/work/ai_daily.log` 확인.
- 코드 상세·파일 위치·미완작업(내부링크A·썸네일B·소재선별C)은 **스킬 `~/.claude/skills/eddie-blog-auto/SKILL.md`** 에 정리됨. 아래는 날짜별 상세 이력.

## 지금까지 (완료)
- **블로그 정체성 전환**: ioiykd8599 = 강점 → **AI 실전 브랜딩**("40대 가장의 AI 생존기", 별명 에디). 유튜브 @AI생존기Edi와 동일. 3역할 확정: 경제(제이)=순수 애드포스트 / 이 블로그=AI 브랜딩 / 한성협=소자본창업.
- **2026 현실**: 네이버 AI 브리핑+저품질 강화로 셀럽·이슈 대량발행 모델 죽음. 생존조건 = AI초안+사람경험. 홈피드는 신뢰·전문·공식성으로 노출.
- **코드 (`~/클로드 코드 블로그 자동화 웹`)**:
  - `gen_ai.py` = AI 실전 반자동 생성기. 제목 홈판패턴 반영(2026-07-19 개선). 본문+[[경험슬롯]]2곳+표+썸네일.
  - `gemini_thumb.py` = **글 전문 → Gemini가 요약·디자인**(`make_from_article`, 기본). 문구지정형(`make_from_thumb`)·HTML형 폴백. gemini-3.1-flash-image(한글 정확).
  - `pipeline.py` = AI글도 [사진N] 이미지 자동첨부(ai.flag 감지), '1단계/2단계'→인용구2(`_apply_step_quotes`), 사진뒤 소주제→66박스.
  - `app_ai.py` = **에디 블로그 자동화 대시보드**(포트 5005). 주제선택→초안생성→편집(경험채우기)→발행. 바탕화면 `에디블로그자동화.command`.
  - AI 초안엔 `{no}_*_ai.flag` 표식 → 대시보드가 옛 셀럽/강점 글과 구분.
- **7시 자동발행 영구정지 (2026-07-23 진짜로 완료)**: ★파일만 `.plist.disabled`로 바꾸는 건 불충분 — launchd DB엔 `enabled`로 남아 매일 7시 셀럽 10편을 계속 생성했었음(7/23 발견). **완전 정지 = `launchctl bootout gui/$(id -u)/com.edi.blogauto` + `launchctl disable gui/$(id -u)/com.edi.blogauto`.** 검증: `launchctl print gui/$(id -u) | grep blogauto` → `disabled`. 지금은 disabled 확정. (셀럽 출력경로 = `~/홈판자료/블로그오토/gen_{date}`, 발행로그 = `~/클로드 코드 블로그 자동화 웹/work/run_daily.log`.)
- 소재뱅크: `~/홈판자료/AI실전_소재뱅크30.md`. SEO자료: `~/홈판자료/네이버SEO_CRank_DIA_핵심자료.md`. 정체성: `~/홈판자료/블로그정체성_AI실전_확정.md`.
- 워크플로: `python3 gen_ai.py "주제"` 또는 대시보드 → gen_{date}/에 저장 → 경험 채우고 → `run_daily.py --folder gen_{date} --only {no}`로 임시저장.

## ★ 새 창에서 할 일 (에디님 요청, 미완료)

**[1] 제목·본문에 '홈판 30개 라이브 공식화' 적용 (부분완료)**
- 제목은 승자패턴을 프롬프트에 넣어 개선함(2026-07-19). 단 gen_celeb/gen_issue처럼 **실시간 홈판 제목 수집→formula.extract_formulas**는 아직 gen_ai에 없음. AI/부업 홈판 디렉토리 찾아 `celeb_sources.fetch_theme_titles` 방식으로 라이브 반영하면 더 강해짐.

**[2] 연예인 + AI 접목 글 생성기 (2026-07-23 완료 → `gen_celeb_ai.py`)**
- `gen_celeb_ai.py` = 셀럽 랭킹뉴스 후킹 → 에디 AI 실전 정보로 정직하게 잇는 반자동 생성기(쉬즈블랑 AI판).
- 핵심 장치: **AI 연결 게이트**(`pick_article` — 연결강도 '중' 이상만, 억지면 RuntimeError로 슬롯 포기), **제목=본문 일치**(TITLE_PROMPT가 셀럽→AI 다리를 정직하게), 정체성은 에디(AI생존기), `[[경험 슬롯]]`+`ai.flag`(대시보드가 AI글로 분류 → C-Rank AI축 유지), 썸네일은 gen_ai와 동일(제미나이 3단 폴백).
- API: `generate(out_dir, no, hint)`, `generate_many(out_dir, count=2, start_no=31)`. CLI: `python3 gen_celeb_ai.py "힌트"`.
- ★TODO: 실발행 전 첫 결과물 품질 눈으로 확인(억지 연결/제목-본문 괴리 없는지). 스모크테스트 폴더 = scratchpad/celebai_test.

**[3] 하루 자동 믹스 (2026-07-24 완료 → `run_ai_daily.py`)**
- 에디님 확정: **하루 3편 = AI 실전 2편 + 연예×AI 1편**(5편에서 축소).
- `run_ai_daily.py`: 01·02=`gen_ai`(소재뱅크에서 중복없이 주제 선택, 이력 `work/ai_topics_used.json`, 30개 소진 시 자동 초기화), 03=`gen_celeb_ai`(억지 연결이면 자동 스킵).
- **전부 임시저장 고정**(`publish=False` 하드코딩) — 완전무인 공개발행 금지 원칙 유지. 경험 슬롯은 에디님이.
- 스케줄: `com.edi.aiblog.plist`(매일 07:00, 프로젝트 폴더에 사본). 로그 `work/ai_daily.log`.
  설치: `cp` → `launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.edi.aiblog.plist` → `launchctl enable`.
- CLI: `python3 run_ai_daily.py [--ai 2] [--celeb 1] [--gen-only] [--dry]`

**카테고리 자동 지정 (2026-07-24 신규)**
- `naver.save_draft(..., category=)` + `pipeline.process_post(..., category=)` 인자 추가. `naver._select_category()`가 발행 팝업에서 카테고리 선택.
- ★**베스트 에포트**: 스마트에디터 클래스 해시가 자주 바뀌어 후보 셀렉터+텍스트매칭으로 방어. 실패해도 기본 카테고리로 임시저장은 정상 진행(글 안 잃음).
- ★**아직 라이브 DOM 검증 안 됨** — 첫 실제 임시저장 때 로그에서 "카테고리 'AI 자동화 실전' 선택 완료"가 찍히는지 확인 필요. 안 찍히면 셀렉터 보정.
- 현재 매핑: AI 실전·연예×AI 둘 다 → `AI 자동화 실전`(run_ai_daily.py 상단 CAT_* 상수에서 변경).

**[4] 슈퍼멤버스 음식 체험 카테고리 (2026-07-23 카테고리명 확정)**
- 카테고리명 = **`AI 에디의 일상`** (에디님 결정). 채널 정체성(AI 에디)을 달아 메인 AI축과 경쟁 안 함 + '일상'으로 곁가지임 명시.
- ★조건: 비중 낮게(전체 발행의 10~15% 이하, AI 글이 항상 다수여야 C-Rank AI축 유지). 이 카테고리 글에서 **AI 교육/랜딩 링크 금지**(오디언스 안 겹침, 스팸 신호). 많이 할 거면 여전히 별도 '일상·맛집' 블로그가 정석.
- 미완: 이 카테고리용 체험 리뷰 생성 흐름은 아직 코드화 안 함(현재는 수동 발행 가정).

## 카테고리 전략 (2026-07-23 에디님 지시)
- **`연예이슈강점` = 옛 연예·강점 모델 글 전부 몰아둔 카테고리. 지금은 절대 삭제·비공개 X.** 그나마 이게 최소 조회수 바닥을 깔아줌. **AI 에디 콘텐츠로 조회수를 올리는 만큼 점진적으로 비워간다**(대체 전략 — 한 번에 밀지 말 것).
- `40대 생존 기록` = '방법'이 아니라 '서사' 칸(실직→AI 재기 경험·감정). 신뢰/저품질회피의 경험 신호원. 살려둘 것.
- 신설 `AI 에디의 일상` = 맛집·체험단 등 가벼운 곁가지(비중 10~15%↓, AI 랜딩 링크 금지). 서사(생존기록)와는 결이 다름.

## 2026-07-31 밤 — velocity 분산·인사말 제거·연예캡처 원복
- **인사말 제거**(홈판 규칙): 홈판 위너는 "안녕하세요 에디입니다"로 시작 안 함. gen_ai 프롬프트 인사 금지 강화 + `pipeline._strip_greeting_intro`(도입 인사·자기소개 줄 자동 제거, 이중). ★제목·썸네일·도입부는 무조건 홈판 공식([[eddie-blog-homefeed-first]]).
- **velocity 분산**(AI 티 방지): 5~6편을 한 번에 임시저장하면 봇/AI로 잡힐 수 있음. `--stagger 1800`(편당 30분) + plist **새벽 4:30 시작**. 전체 생성 후 편당 30분 간격 임시저장. naver 타이핑 딜레이도 상향(사람처럼 천천히). ★Mac이 깨어 있어야 함(sleep 시 launchd 타이머·2.5h 프로세스 안 돎).
- **연예 캡처 원복**: 네이버 이미지(기사 화보)→유튜브(방송) 썸네일. 에디님 방침: 자막 OK, 기사 아님, 출처만 정확히. `celeb_image.py` 유튜브 버전.

## 2026-07-31 저녁 — ★최종 확정: AI 홈판 블로그 + 연예90/10 온램프
- 에디님 결론: 이 블로그는 **AI 블로그**로 홈판을 노린다. 연예 별도 블로그(예전 회귀)도, 순수 연예 다편(연예 블로그화)도 아님. **AI 홈판 위주 + 연예90/AI10 딱 1편**(트래픽 온램프, AI 10%로 AI 블로그에 걸침).
- 하루 = **AI 홈판 4편 + 연예90/AI10 1편**(plist `--ai 4 --celeb 1`, 재설치·enabled). celeb_pure/story/ai_hook는 코드에 남지만 미사용. 기본 연예 모드 = `celeb_ai10`.
- `BODY_PROMPT_AI10`: 90% 순수 연예 + 맨 끝 AI 1문단(2~3문장, "이런 이슈 AI로 정리해봄" 수준, 튜토리얼·자랑 X). 제목=순수 연예(TITLE_PROMPT_PURE). 검증: 나솔 영자 이혼설글, 90/10 비율 정확·자연.
- ★사망·범죄·비방 제외(에디님 지시): PICK_PROMPT_PURE 문구 + `_BLOCK_RE` 안전망(선정 후 제목/후킹 키워드 재검). 이혼·결별·근황은 통과.
- 블로그 명: **`AI에디` 확정**(에디님 2026-07-31 저녁). 별명 '에디'. 확정본 문서 반영. 에디님이 네이버 관리에서 블로그명 변경.
- **AI 주제 홈판 검증 완료**: `gen_ai.derive_topics`가 이제 전체 홈판(dir=0, home_titles) + AI트렌드를 함께 보고 '무조건 홈판 뜰' 주제만 도출. DAILY_TOPIC_PROMPT에 홈판검증 3기준 + 니치/전문 주제 탈락(API·벤치마크·프롬프트이론 X). 검증: 여행·부업·신제품에 AI 붙인 홈판형 주제 나옴.

## 2026-07-31 — 1순위 소재 능동 확보(검색 풀)
- 에디님: 나솔·나솔사계·이혼숙려·이호선·오은영이 연예이슈강점 최고 조회수 소재. 이걸 순수 연예에 확실히 넣어라.
- 문제: 오늘 랭킹뉴스에 없으면 1순위여도 못 잡힘.
- 해결: `celeb_sources.fetch_news_search(query)`(네이버 뉴스검색 스크래핑, 제목에 소재 있는 것만=관련도) + `gen_celeb_ai.build_pure_pool()`(랭킹30 + 1순위 소재 검색 → dedup). run_ai_daily가 풀 1회 구성해 연예 글들이 공유(`news` 파라미터). 검색기사는 url='' → 스니펫을 본문 소재로(_write_one이 url 유무 분기). 검증: 나솔 영자 결별글이 검색 풀에서 선정, AI 0, 지어내기 없음.
- ★한계: 검색 스니펫은 ~200자로 얇음(랭킹기사는 전문). 이호선은 교수 동명이인(교보증권 세미나) 걸리기도 — 필요 시 PICK가 거름.

## 2026-07-31 — ★역할 분리(C): 순수 연예 + 순수 AI 두 엔진
- 지적: 연예×AI 하이브리드가 홈판용으로 어중간(연예 몰입·AI 정보성 둘 다 약함). 에디님 결정 = C(역할 분리, 한 블로그 두 엔진).
- 연예 = **순수 홈판 트래픽**(`gen_celeb_ai` mode='celeb_pure', AI 0%): AI·에디·홍보 전부 제거. 제목 홈판식 강한 후킹, 본문 순수 연예 이슈 정리(DIA+), 사진 자리표시자+유튜브 캡처, 끝 출처, 1순위 우선, AI 게이트 없음. ai.flag 안 붙임. 프롬프트 `BODY_PROMPT_PURE`·`TITLE_PROMPT_PURE`·`PICK_PROMPT_PURE`. 검증: 진태현 하차글 AI언급 0.
- AI = 순수 AI 실전글(gen_ai) 그대로. 카테고리: 연예=`연예이슈강점`(run_ai_daily `CAT_CELEB`, 새 칸 원하면 상수 교체) / AI=categorize().
- run_ai_daily: 6편=AI 2 + 순수연예 4(celeb_story/ai_hook 폐기). 옛 mode(ai_hook/celeb_story)·PROMPT는 코드에 남아있으나 미사용.
- ★C-Rank 주의: 한 블로그에 연예+AI 섞으면 두 축 권위 다 약해질 수 있음(정석은 연예 별도 블로그). 에디님이 감수하고 진행.

## 2026-07-27 — 전체 홈판(dir=0) 공식 + 연예인 사진 캡처 + 연예 본문 공식
- 에디님 지시: 무조건 지금 홈판을 검색해 잘 나가는 주제·제목·썸네일 공식으로. 본문도 잘되는 글(AI쪽/연예쪽) 공식으로. 연예 사진은 자리표시자+캡처(유튜브/인스타).
- Build1 (`gen_ai.collect_live_context`): 주제=dir=30(AI), **제목·썸네일 공식=dir=0(전체 홈판)**, AI 본문 공식=dir=30. `DIR_HOME=0` 추가. `ctx['home_titles']`.
- Build2 (`celeb_image.py`): 유튜브 검색→상위 영상 썸네일 캡처. gen_celeb_ai가 연예 글마다 `{no}_{kw}_연예캡처N.jpg` 2장 저장(자리표시자 유지). 검증: 나솔·이혼숙려·이호선·공효진 실제 이미지. ★한계: '관련 인물'은 맞으나 특정 장면 매칭은 안 됨(인기영상 상위). 인스타는 안티봇으로 미구현. 저작권은 에디님 판단.
- Build3 (`gen_celeb_ai.collect_celeb_body_formula`, dir=12): 연예 글 본문이 연예 홈판 위너 구조 따르게. BODY_PROMPT 2종에 `{body_formula}` 슬롯. run_ai_daily가 1회 추출 공유.
- ★watch: 썸네일이 가끔 영어로 나옴(Gemini 드리프트). 실플로우는 thumb_formula(한글 dir=0)로 제약되나 발행 전 확인 권장. Gemini 429(일일 한도) 시 텍스트 폴백.

## 2026-07-27 — 방향 정정: 브랜딩 X, 완전 홈판 수익형 O
- 07-25 '자동화 파는 브랜드' 방향이 과했음. 에디님: "글이 약간 브랜드로 가는데, 나는 완전 홈판으로 수익 얻는 블로그. 신뢰 주되 완전 홈판 노림."
- 목적 = 홈판(홈피드) 트래픽 → 애드포스트 수익. 신뢰(경험)는 유지, 자기 브랜딩·상품 판매는 뺌.
- 수정: `EDI_STORY`·`EDI_AI_PROFILE`(gen_ai·gen_celeb_ai)에서 "시스템 판매 지향/바이브코딩 강의/1인 사업자" 제거, '독자에게 실전 정보 주기'로. `DAILY_TOPIC_PROMPT` 축을 '내 시스템 자랑'→'검색·클릭 나는 실전 주제(트래픽)'로. `소재뱅크` 재조정(브랜드 B그룹·교육퍼널 F그룹 제거, 초보 how-to·트렌드연결로). 유튜브 언급은 글 끝 1회만. 확정본 문서 상단에 정정 명시.
- 신뢰(경험)는 그대로: 40대 밑바닥→AI 실전, 함정도 앎.

## 2026-07-27 — 카테고리 내용별 자동 배정 (매번 같던 문제)
- 문제: 6편이 전부 'AI 자동화 실전' 하드코딩 → 카테고리 매번 동일, 다른 카테고리 방치.
- 수정: `run_ai_daily.categorize(text)` — 주제/AI주제 키워드로 배정. `vs·비교·리뷰·후기·툴` → **AI 툴 리뷰·비교** / `수익·부업·전자책·팔아·월급` → **AI 수익화·부업** / 그 외 → **AI 자동화 실전**. AI글=topic, 연예글=ai_topic 기준. CAT_AI/CAT_CELEB_AI 상수 대체.
- ★미검증: `_select_category`가 '수익화·부업'·'툴 리뷰·비교'(· 중점 포함) 실선택 되는지는 아직 로그로 확인 필요(자동화 실전만 검증됨). 이름 불일치 시 한 글자 수정. 실패해도 기본 카테고리로 임시저장은 정상.

## 2026-07-27 — 하루 6편으로 (연예 70/30 제목 스타일 분리)
- 구성: AI 2 + 연예 4(AI중심 2 + 70/30 스토리 2). plist `--ai 2 --celeb 4 --celeb-story 2`(재설치·enabled 확인).
- 스토리 2편 제목 분리: 첫 편=제목에 AI 포함 / 둘째=순수 연예 제목. `gen_celeb_ai.generate(story_title_ai=)` + `_AI_TITLE_SIG`로 후보 중 조건 맞는 것 선택. run_ai_daily가 story_seen==0만 True.

## 2026-07-25 — 연예 제목 '나 vs 연예인 비교형' 금지
- 문제 제목: "민도희도 카페 알바…40대 나는 AI로 부업 만들었다" → ①무명 화자를 연예인과 대등 비교 ②연예인 어려움(생계·알바)을 깔고 내 성공 자랑 = 무례/이상. 원인 = TITLE 프롬프트 예시가 "연예인만? 나도" 비교형이었음.
- 수정: `gen_celeb_ai` TITLE_PROMPT·TITLE_PROMPT_STORY 예시를 '연예=후킹의 문, 약속은 독자에게' 형으로 교체 + ★절대금지(나/나도 비교, 어려움 미끼 자랑, 대등 배치). BODY_PROMPT 2종에도 같은 톤 가드 추가.

## 2026-07-25 — 연예 선정 1순위 지정
- 에디님 지정: 나는솔로·나솔사계·이혼숙려캠프·이호선 교수·오은영 교수를 연예×AI 선정 **1순위**로.
- 반영: `gen_celeb_ai.PRIORITY_SUBJECTS` + PICK_PROMPT에 "기사 목록에 있으면 최우선(단 AI로 이을 수 있을 때), 없으면 일반 기준" 주입. 억지 선택 X. 관계·상담·자기계발 결이라 AI(자기계발/상담/궁합/자기소개)로 잇기 자연스러움.

## 2026-07-25 낮 — 정체성 업그레이드: '자동화를 만들고 파는 사람'
- 지적: 블로그가 "완전자동은 망한다, 반자동이 답"이라 말하는데 이건 에디님 실제 전문성·상품과 충돌(에디님은 자동화 시스템을 만들고 팔 사람). 발행방식 아니라 **콘텐츠 포지셔닝** 문제.
- 실제 정체성(근거 = `~/에디비즈니스/00_현황과전략.md`·`02_당근AI모임_브리핑`): 밑바닥→AI/바이브코딩으로 블로그·유튜브·인스타·스레드·웹사이트 완전자동 시스템 구축·운영. 당근에서 유료 강의(유튜브·블로그·전자책·동물쇼츠, 완판·대기). 바이브코딩 강의 준비 중. **1인 사업자 마케팅 자동화 시스템 판매 지향(아직 진행형 — 판매 전, 가짜수치 금지).**
- 반영: `gen_ai.EDI_STORY`(성장서사+시스템빌더+지향 명시)·`EDI_AI_PROFILE`·`DAILY_TOPIC_PROMPT`(에디 축에 바이브코딩·1인사업자·"내가 만든 시스템 이렇게 작동" 추가)·`gen_celeb_ai` 프로필·`AI실전_소재뱅크30.md`(B그룹 '바이브코딩·완전자동 시스템' 신설, 자동화=실패 프레이밍 제거). 확정본 `블로그정체성_AI실전_확정.md` 업데이트.
- ★"반자동"은 네이버 블로그 한정 전술로 재배치. 퍼널은 소프트만(전문성 노출, 하드 판매 링크 X — C-Rank).

## 2026-07-25 아침 — AI 주제도 매일 홈판 트렌드에서 (반복 탈피)
- 지적: "매일 왜 똑같은 주제야?" → 원인 = AI 실전 2편이 **고정 소재뱅크(30개)** 순서 소진.
- 수정: `gen_ai.derive_topics(ctx, n, used)` — 오늘 홈판 IT·컴퓨터 위너 제목 30개에서 에디 축(AI×실전)으로 **오늘의 글감 도출**. `run_ai_daily`가 이걸 먼저 쓰고 부족하면 소재뱅크 폴백. 최근 쓴 주제(ai_topics_used.json) 제외.
- 검증(7/25 트렌드): "구글·SK하이닉스 실적 쏟아진 날, AI한테 분기실적 요약시켜 카드뉴스 만들기" / "'갤럭시워치9 vs 울트라2' 비교글 잘 나온다길래 AI로 비교표 자동으로" → 트렌드 반영 확인.
- **썸네일 레퍼런스 공식화 (2026-07-25 완료)**: `celeb_sources.fetch_theme_thumbnails`(홈판 대표 썸네일 URL 수집) → `gemini_thumb.extract_thumb_formula`(비전으로 위너 6장 공통 공식 추출) → `make_from_article(ref_formula=)`에 주입. `collect_live_context`가 ctx['thumb_formula']로 담아 gen_ai·gen_celeb_ai 공유. 검증: 어두운 현장 배경+노랑 강조 1색+키워드 대형화로 실제 생성됨(기존 자유디자인보다 트렌드 밀착). 실패 시 자유 디자인 폴백.
- → 에디님 3요소(홈판 최신 주제 / 제목30 공식 / 레퍼런스 공식으로 글·썸네일) 전부 반영 완료.

## 2026-07-25 새벽 — 5편 첫 실행 버그픽스
- **연예글 인물 중복**: run_ai_daily가 연예 3편을 각각 독립 pick → 매번 1위(최화정)만 골라 3편 다 최화정이 됐음. 수정: `used_persons` 공유 + `pick_article(exclude_persons=)` + PICK_PROMPT에 '이미 다룬 인물 제외' 주입 + 재선정 시 스킵.
- **제목 라벨 누수**: `[셀럽 후킹] …`가 제목이 됨. `gen_ai._clean_title`(번호·불릿·`[라벨]` 제거) 공용화, gen_celeb_ai도 사용.
- ※7/25 07:00 자동실행분부터 반영. (7/25 수동 실행분 03·04·05는 최화정 중복 — 정리 필요)

## 2026-07-24 저녁 — 하루 5편 + 연예70/AI30 모드 + 연예사진 자리표시자
- **하루 5편**: AI 실전 2 + 연예×AI 3(그중 1편은 연예70/AI30 스토리모드). plist `--ai 2 --celeb 3 --celeb-story 1`. ★설치된 plist 갱신 필요(bootout→cp→bootstrap).
- **연예70/AI30 모드**(`gen_celeb_ai` mode='celeb_story'): 연예 이야기 70% + 뒤에 AI 30%. `BODY_PROMPT_STORY`/`TITLE_PROMPT_STORY`. 연결 게이트 완화(relaxed). 검증: 지무비×권아름 열애 스토리 → AI 리뷰채널 접목, 비율 정확.
- **연예인 사진 자리표시자**(에디님 지시): `pipeline._is_celeb_photo` — 출처가 유튜브/방송/인스타면 자동첨부 안 하고 `[사진N]` 마커로 남김(에디님이 실제 캡처 삽입). 출처=직접 촬영이면 자동첨부. → 실제 방송캡처는 AI표기서도 자연 제외(마커라 삽입 안 됨). gen_celeb_ai 프롬프트가 두 출처를 안 섞게 지시.
- **연예인 글 끝 출처 필수**: `gen_celeb_ai._ensure_source_line` — 맨 끝 `@출처 : 매체` 보장(media 비면 프로그램/기사 폴백, 누락 시 삽입).
- **제목 필터 강화**: 모델이 앞에 붙이는 안내문구('제목 5개입니다' 등)가 제목이 되던 버그 수정(gen_ai·gen_celeb_ai `bad` 정규식).

## 2026-07-24 오후 보정 (실사용 피드백 반영)
- **존댓말 고정**: `gen_ai`·`gen_celeb_ai` 프로필+본문규칙에 존댓말 명시. ★"홈판 공식은 구조만 참고, 어투는 따라가지 말 것" 못박음(레퍼런스 주입이 반말을 끌고 왔던 사고). → [[blog-tone-lock]]
- **홈판 실시간 공식**: `gen_ai.collect_live_context()`가 IT·컴퓨터 디렉토리(=30, 실측)의 위너 제목·본문을 읽어 `formula.extract_formulas`로 그날 공식 추출 → 제목/본문 프롬프트에 주입. 여러 편이 1회 공유. (구조가 매일 똑같던 문제 해결. 단 도입부는 아직 비슷 — 추가 다양화 여지)
- **경험 문단 직접 작성**: 빈 `[[경험 슬롯]]` 대신 `EDI_STORY`(실직·폐업·코딩0·AI재기·반자동 저품질경험)를 프롬프트에 넣어 주제별 1인칭 경험문단을 직접 쓰게 함. 지어낸 수치 금지, 선택 슬롯 `[[경험 보태기:]]` 하나만.
- **유튜브 썸네일**: `youtube_thumb.py`(채널 RSS→최신영상 썸네일). pipeline이 유튜브 안내문구 뒤에 이미지 삽입(링크 X, 네이버가 외부링크 싫어함).
- **AI 활용 표기(중요)**: '전체AI활용' 텍스트버튼은 에디터 iframe에서 DOM쿼리로 안 잡힘(상위 프레임 추정). ★실제 동작 = `naver._mark_all_ai`가 이미지마다 `button.se-set-ai-mark-button-toggle`을 순회하며 ON(이미 켜진 건 스킵). 라이브 검증됨(se-is-selected). 빠른경로(전체AI활용)는 시도만 하고 실패 시 이 폴백.
- **첫째/둘째/셋째 등 연속 열거 머리줄** → 인용구2(라인형). `pipeline._STEP_RE` 확장.
- **버그픽스**: `_apply_orphan_subheads`가 `@출처`·해시태그 줄을 인용구로 바꾸던 것 제외 처리.

## 주의(메모리에도 있음)
- Desktop 금지(TCC). 완전무인 금지(저품질). claude -p 느릴 땐 백그라운드로. 지어낸 수치·경험 금지. 사진출처는 유튜브에서 찾게 구체적으로.
